const initialState= {
    name: "",
    age: "",
    height: "",

}

export const smurfReducer = (state = initialState, action) =>{
    switch(action.type){
        default:
            return state
    }
}